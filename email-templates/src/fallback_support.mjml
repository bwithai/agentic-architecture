<mjml>
  <mj-body background-color="#fafbfc">
    <mj-section background-color="#fff" padding="40px 20px">
      <mj-column vertical-align="middle" width="100%">
        <mj-text align="center" padding="35px" font-size="20px" font-family="Arial, Helvetica, sans-serif" color="#333">{{ project_name }} - Support Request</mj-text>
        <mj-text align="left" font-size="16px" padding-left="25px" padding-right="25px" font-family="Arial, Helvetica, sans-serif" color="#555">
          <p>A user query could not be answered with sufficient confidence by the chatbot:</p>
          <p><strong>User:</strong> {{ user_query }}</p>
          <p><strong>Chatbot Response:</strong> {{ chatbot_response }}</p>
          <p><strong>Confidence Score:</strong> {{ confidence_score }}</p>
          <p>Please review and follow up with the user as necessary.</p>
        </mj-text>
        <mj-divider border-color="#ccc" border-width="2px"></mj-divider>
        <mj-text align="center" font-size="14px" padding-left="25px" padding-right="25px" font-family="Arial, Helvetica, sans-serif" color="#999">
          This is an automated notification from {{ project_name }}.
        </mj-text>
      </mj-column>
    </mj-section>
  </mj-body>
</mjml> 